#
# misc hotkeys
#

# terminal emulator
super + Return
    	gnome-terminal

# program launcher
super + d
        dmenu_run -fn 'Ubuntu Monospace-10' -nb '#222' -sf '#222' -sb '#EBDBB2' -nf '#EBDBB2'

# make sxhkd reload its configuration files:
super + Escape
	pkill -USR1 -x sxhkd

# kill whatever
super + x
        xkill



#
# bspwm hotkeys
#

# quit bspwm normally
super + alt + Escape
	bspc quit

# close and kill
super + {_,shift + }w
	bspc node -{c,k}



#
# focus/swap
#

# focus the node in the given direction
super + {_,shift + }{Left,Down,Up,Right}
	bspc node -{f,s} {west,south,north,east}

# focus the next/previous node in the current desktop
super + {_,shift + }n
	bspc node -f {next,prev}.local

# focus or send to the given desktop
super + {_,shift + }{1-9,0}
	bspc {desktop -f,node -d} '^{1-9,10}'



#
# preselect
#

# preselect the direction
super + ctrl + {Left,Down,Up,Right}
	bspc node -p {west,south,north,east}

# preselect the ratio
super + ctrl + {1-9}
	bspc node -o 0.{1-9}



#
# move/resize
#

# expand a window by moving one of its side outward
super + alt + {Left,Down,Up,Right}
	bspc node -z {left -30 0,bottom 0 30,top 0 -30,right 30 0}

# contract a window by moving one of its side inward
super + alt + shift + {Left,Down,Up,Right} 
	bspc node -z {right -30 0,top 0 30,bottom 0 -30,left 30 0}

# move a floating window
super + ctrl + {Left,Down,Up,Right}
	bspc node -v {-30 0,0 30,0 -30,30 0}

# rotate
super + ctrl + r
    bspc node @/ -R 90



#
# audio
#
super + z
    amixer sset Master 5- unmute

super + x
    amixer sset Master 5+ unmute


#
# brightness
#
alt + {1-9}
    xrandr --output eDP-1 --brightness 0.{1-9}

alt + 0
    xrandr --output eDP-1 --brightness 1.0

super + shift + z
    xrandr --output eDP-1 --brightness 0.5

super + shift + x
    xrandr --output eDP-1 --brightness 1.0



#
# bookmarks
#
super + ctrl + f
    google-chrome --app=https://m.facebook.com

super + ctrl + g
    google-chrome --app=https://www.google.co.uk

super + ctrl + r
    google-chrome --app=https://www.reddit.com

super + ctrl + n
    google-chrome --app=https://www.netflix.com

super + ctrl + y
    google-chrome --app=https://www.youtube.com

super + ctrl + m
    google-chrome --app=https://mail.google.com

super + ctrl + k
    google-chrome --app=https://keep.google.com

super + ctrl + s
    google-chrome --app=https://sainsburys.co.uk

super + ctrl + i
    google-chrome --app=https://www.bbc.co.uk/iplayer

super + ctrl + e
    google-chrome --app=https://www.reddit.com

super + ctrl + h
    google-chrome --app=https://www.reddit.com

super + ctrl + j
    google-chrome --app=https://www.just-eat.co.uk

super + ctrl + i
    google-chrome --app=https://www.reddit.com

#
# applications
#
super + shift + n
        gnome-terminal -x bash -c "newsbeuter -r; exec bash"

super + shift + e
        gnome-terminal -x bash -c "mutt; exec bash"

super + shift + v
        gnome-terminal -x bash -c "vim; exec bash"

super + shift + f
        gnome-terminal -x bash -c "ranger; exec bash"

super + shift + r
        gnome-terminal -x bash -c "rtv --enable-media; exec bash"

super + shift + h
        gnome-terminal -x bash -c "htop; exec bash"

super + shift + t
        gnome-terminal -x bash -c "htop; exec bash"

super + shift + s
        GDK_SCALE=2 steam

super + shift + m
        gnome-terminal -x bash -c "ncmpcpp; exec bash"

super + shift + c
        gnome-terminal -x bash -c "bc; exec bash"

super + shift + y
        gnome-terminal -x bash -c "ranger ~/Videos/youtube/; exec bash"

super + shift + l
        gnome-terminal -x bash -c "lynx 'https://duckduckgo.com'; exec bash"